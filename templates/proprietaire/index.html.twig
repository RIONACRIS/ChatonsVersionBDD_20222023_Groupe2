{% extends 'base.html.twig' %}

{% block title %}Hello ProprietaireController!{% endblock %}

{% block body %}
    <table class="table table-striped">
        <tr>
            <th>Nom</th>
            <th>Prenom</th>
            <th></th>
        </tr>
        {% for c in proprietaires %}
            <tr>
                <td>{{ c.nom }}</td>
                <td>{{ c.prenom }}</td>
                <td>
                    <a href="#" class="btn btn-primary">modifier</a>
                    <a href="#" class="btn btn-danger">supprimer</a>
                </td>
            </tr>
        {% endfor %}
    </table>
    <a href="{{path("app_proprietaire_ajouter")}}" class="btn btn-success">Ajouter un proprietaire</a>
{% endblock %}
